client端添加异步处理用户输入信息机制，并增加uloop机制。
nick
2019-01-02

添加命令行输入和消息接受异步处理机制，实现server端发送、接受的异步处理。
2019-01-01
Nick

this is a test project for the socket and uloop.
2018-12-31
Nick


环境说明：
Firstly, we must install the libubox to the ubuntu.
###get the libubox source code.
sudo mkdir -p /opt/git/libubox
sudo git clone git://nbd.name/luci2/libubox.git /opt/git/libubox
cd /opt/git/libubox/
sudo apt-get install cmake
sudo cmake CMakeLists.txt -DBUILD_LUA=OFF
sudo make 
sudo make install
ldconfig




